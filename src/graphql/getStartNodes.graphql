query getStartNodes {
  queryPerson(filter: { name: { allofterms: "Pa Salt" } }) {
    ...PersonFields
    story {
      title
    }
    children {
      ...PersonFields
    }
    parents {
      ...PersonFields
    }
    nonBioChildren {
      ...PersonFields
    }  
  }
}
