type Geo {
    lon: Float
    lat: Float
}

type Person {
    personID: ID!
    name: String @search(by: [term])
    nickNames: [String]
    # just year for now
    dateOfBirth: Int 
    # just year for now
    dateOfDeath: Int
    visited: [Geo]
    related: [Person]
}

type Story {
    title: String
}

# type Edge {
#     directional: Boolean
#     # e.g. adoptive father
#     type: String
# }
